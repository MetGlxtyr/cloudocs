import { http } from './http'
import { getToken, setToken, removeToken } from './token';
import {
  HistoryRouter,
  history
} from './history';

export {
  http,
  getToken,
  setToken,
  removeToken,
  HistoryRouter,
  history
}